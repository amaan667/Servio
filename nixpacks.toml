# Railway/Nixpacks configuration
# Playwright is not needed in production, only for local testing

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm run build"
]
