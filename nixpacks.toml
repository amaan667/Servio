# Nixpacks configuration to prevent metal build environment
[phases.setup]
nixPkgs = ["nodejs-18_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["NODE_NO_WARNINGS=1 pnpm run build"]

[start]
cmd = "pnpm start"

