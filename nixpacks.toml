# Nixpacks configuration for Railway
# Simple config: let Nixpacks detect everything but override phases

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["NODE_NO_WARNINGS=1 pnpm run build"]

[start]
cmd = "pnpm start"
