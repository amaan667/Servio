[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["bash scripts/pre-build.sh", "pnpm run build"]

[start]
cmd = "pnpm start"

[variables]
NODE_ENV = "production"
PORT = "8080"
