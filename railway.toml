# Minimal Railway config: let Nixpacks auto-detect.
[build]
builder = "NIXPACKS"   # lets platform use default Nixpacks
# No custom buildCommand; nixpacks.toml will drive phases.

[env]
NODE_ENV = "production"
